<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
	xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	xmlns:local="clr-namespace:MindwaveGamePlayerSetup;assembly=MindwaveGamePlayerSetup"
	xmlns:controls="clr-namespace:XLabs.Forms.Controls;assembly=XLabs.Forms"
	x:Class="MindwaveGamePlayerSetup.PlayerEditPage">
	<ContentPage.ToolbarItems>
		<ToolbarItem Command="{Binding SavePlayerCommand}" Text="Save" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<TableView Intent="Form">
			<TableRoot>
				<TableSection>
					<EntryCell Label="Name" Text="{Binding Name}"></EntryCell>
<!--					<ViewCell>
						<ViewCell.View>
							<controls:BindablePicker Title="Headset" ItemsSource="{Binding Headsets}" SelectedItem="{Binding HeadsetId}" SourceItemLabelConverter="{Binding GetText}" />
								<local:MyBindablePicker Title="Headset" ItemsSource="{Binding Headsets}" SelectedItem="{Binding HeadsetId}" SourceItemLabelConverter="GetText()" />
						</ViewCell.View>
					</ViewCell>-->
				</TableSection>
			</TableRoot>
		</TableView>
	</ContentPage.Content>
</ContentPage>
